name: Say Hello

on:
  push:
    branches:
      - develop

jobs:
  hello_world:
    runs-on: ubuntu-latest
    steps:
      - name: Echo the message
        run: |
          echo "Hola Alex!"
      - name: Echo the message
        run: |
          echo "Que tal?"

